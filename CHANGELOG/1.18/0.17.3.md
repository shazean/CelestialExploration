- `[FIX]` Fixed broken Patchouli texture
- `[FIX]` Fixed workbench and oxygen compressor not using a tool tag, and therefore taking forever to break no matter if you used an axe or a pickaxe. Oxygen compressor now breaks faster when using a pickaxe, and workbench can be used with either tool. Since it's half wood and half stone.
- `[CHG]` Added block/ingot amounts to JEI tooltip for workbench recipes, for those who find millibucket (mb) amounts confusing
- `[FIX]` Fixed a not-implemented-yet-but-code-half-exists entity still using Azurelib 2.x code, therefore breaking the mod if trying to use Azurelib 3.1.x and above.
- `[CHG]` Added a windowed hinge version for both hangar and airlock panel doors, just in case you want the hinge block to have a window also, because it previously looked the same as the non-window version.  Also fixed some wonky textures/UV mapping with said windows for the hangar doors.
- `[FIX]` Fixed the magcart model being rotated 90º and not always turning when the rail turns